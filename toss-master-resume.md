# 김현우 — 백엔드 엔지니어

**서울특별시 중랑구** / +82 9625 2784 / kkepm012@naver.com / 1996.devjiinu@gmail.com
[GitHub](https://github.com/UjiinEatingTangerines) · [LinkedIn](https://www.linkedin.com/in/hyunwoo-kim-9a295420b/) · [Blog](https://your-friendly-neighborhood.com/)

---

## Profile

6년간 도시가스·전기차 충전 도메인에서 결제·정산·크레딧 시스템을 설계하고 운영해 온 백엔드 엔지니어입니다. **데이터 정합성**과 **제한된 인프라에서의 안정성 확보**를 엔지니어링의 가장 중요한 기준으로 삼아 왔습니다.

저맥락 커뮤니케이션을 중요하게 생각합니다. 누가 읽어도 같은 의미로 해석되는 문서와 코드를 지향하고, 동료와의 명확한 소통으로 팀이 같은 방향을 향하는 것을 우선시합니다.

단순히 도구를 잘 사용하는 것에 머무르지 않고, 필요한 도구를 직접 설계해 동료도 쉽게 사용할 수 있는 형태로 만드는 일에 가치를 둡니다. 개인의 역량을 넘어, 조직의 엔지니어링 생태계를 넓히고 부가가치를 만들어내는 엔지니어를 목표로 하고 있습니다.

---

## Technical Skills

| 분류 | 기술 |
|------|------|
| **Backend** | Java, Kotlin / Spring Boot, Spring Batch / JPA, QueryDSL, MyBatis |
| **DB & ORM** | MySQL, PostgreSQL / JPA Entity 설계, Fetch Join, 커버링 인덱스, 비관적 락 |
| **Infra & Message Queue** | AWS SQS, EventBridge Scheduler / MQTT(EMQX), Kinesis |
| **DevOps & Tooling** | Git, GitHub Packages, Nexus / gRPC, Protobuf / Jira 자동화, PR 알림 봇 |

---

## Selected Problem Solving Cases

### 1. 비관적 락 기반 크레딧 잔액 정합성 확보 및 SDK 통합

> Spring Boot, JPA, QueryDSL, GitHub Packages, Nexus

**Situation**
크레딧 적립·사용·차감 처리 시 동시 요청에서 잔액 정합성이 깨지는 케이스가 월 12건 발생. 기존 낙관적 락은 충돌 시 재시도 로직이 누락된 상태에서 잔액 불일치를 유발. 또한 내부 서비스마다 크레딧 연동 방식이 제각각이라 신규 서비스 통합 시 매번 2주 이상의 연동 개발이 필요.

**Task**
금융 원장 수준의 잔액 정합성을 보장하면서, 다수 서비스의 크레딧 연동 비용을 구조적으로 제거.

**Action**
- **비관적 락 + 트랜잭션 경계 재설계.** 적립·사용·차감을 단일 트랜잭션으로 묶고 `SELECT FOR UPDATE`로 잔액 행을 잠가 동시성 문제를 근본 차단. 분산 락(Redis)은 단일 DB 내 정합성이면 충분하여 운영 복잡도 대비 이점이 없다고 판단.
- **QueryDSL 기반 동적 조회 + 커버링 인덱스.** 기간·유형·상태 복합 조건 조회가 잦은 이력 API에서 MyBatis 동적 SQL의 XML 유지보수 한계를 QueryDSL로 전환하고, 커버링 인덱스를 조합해 조회 성능 확보.
- **SDK 패키징으로 연동 비용 제거.** API 경계를 명확히 정의하고 클라이언트 SDK를 GitHub Packages / Nexus로 배포. 서비스별 직접 HTTP 연동을 SDK 의존성 추가로 대체.

**Result**
- 잔액 불일치 발생 **월 12건 → 0건**
- 이력 조회 API p95 **1,200ms → 180ms**
- 신규 서비스 연동 리드타임 **2주 → 2일**

---

### 2. 전자금융업 등록을 위한 폐쇄망 이관 및 N+1 / 인덱스 최적화 (제로 다운타임)

> Spring Boot, JPA, QueryDSL, MyBatis

**Situation**
전자금융업 등록 요건상 크레딧·마일리지 시스템을 폐쇄망(망분리)으로 이관해야 하는데, 레거시 API에 N+1 쿼리와 비효율 인덱스가 다수 존재하여 이관 후 제한된 인프라 자원에서 SLO 달성이 불가능한 상태. 카드번호 기준이던 데이터 모델을 멤버 아이디 기준으로 전환하며 API 경계까지 재설계해야 하는 복합 작업.

**Task**
인프라 제약이 강한 폐쇄망 환경에서도 SLO를 충족하도록 API 성능을 최적화하고, 다운타임 없이 전체 시스템을 이관.

**Action**
- **N+1 쿼리 제거 및 Fetch Join 전환.** JPA 엔티티 연관관계 로딩 전략을 재설계. LAZY 로딩 후 루프 내 추가 쿼리가 발생하던 패턴을 Fetch Join / `@EntityGraph`로 일괄 전환.
- **QueryDSL 마이그레이션 + 복합 인덱스 재구성.** MyBatis XML 기반 복합 조건 쿼리를 QueryDSL로 전환하면서, 실행 계획 분석 기반으로 복합 인덱스를 재설계.
- **무중단 DB 마이그레이션 전략 설계·실행.** 데이터 정합성 검증 스크립트를 사전 작성하고, 단계적 트래픽 스위칭(읽기 전환 → 쓰기 전환)으로 전환 완료. 롤백 시나리오별 절차서를 사전 수립.
- **API 경계 재설계.** Admin 및 앱 백엔드가 DB 직접 접근 대신 API 호출 기반 CRUD로 처리하도록 구조를 변경. 종속성은 Nexus로 이관해 이후 변경을 서비스 경계 안에서 흡수.

**Result**
- 주요 조회 API 쿼리 수 **47회 → 4회** (N+1 제거)
- 조회 API 평균 응답속도 **960ms → 120ms**
- DB 마이그레이션 다운타임 **0분**
- 전자금융업 등록 인프라 요건 충족 완료

---

### 3. SQS/EventBridge 기반 비동기 정산·결제 배치 아키텍처 구축

> Spring Boot, Spring Batch, MyBatis, AWS SQS, EventBridge

**Situation**
일별 정산 집계를 단일 쿼리로 처리하던 구조에서 결제 트랜잭션 증가에 따라 배치 수행 시간이 선형 증가, 야간 배치 윈도우를 초과하기 시작. 정산 오류 발생 시 수동 재처리로 익영업일까지 지연되는 운영 리스크 존재. 크레딧 일괄 충전 역시 장시간 동기 처리에 의존해 운영자가 직접 대기해야 하는 상황.

**Task**
정산 배치의 처리 시간을 야간 윈도우 안에 수용하고, 오류 재처리를 자동화하며, 크레딧 일괄 충전의 운영 병목을 제거.

**Action**
- **Spring Batch Chunk 기반 분할 처리.** 단일 대량 쿼리를 MyBatis Cursor + Chunk 처리로 전환하여 메모리 사용량을 일정하게 유지하면서 처리량 확보. Partitioning은 정산 특성상 날짜 단위 순차 정합이 필요하여 Chunk 단위 처리가 적합.
- **SQS + EventBridge로 비동기 이벤트 기반 정산 트리거 구축.** 결제 완료 이벤트를 SQS로 수신하고, EventBridge 스케줄로 일별·월별 정산 Job을 트리거. 수동 크론 관리를 제거하고 재처리 시 SQS DLQ에서 실패 이벤트를 자동 재투입.
- **대사(reconciliation) 로직을 별도 Step으로 분리.** 정산 집계와 대사를 하나의 Job 내 별도 Step으로 구성하여, 대사 실패 시 집계 롤백 없이 대사 Step만 재실행 가능하도록 설계.
- **크레딧 일괄 충전 비동기 배치 전환.** Spring Batch 기반 비동기 배치로 재구현해 운영자 대기 구간과 실제 대량 처리 구간의 실행 경계를 분리.

**Result**
- 야간 정산 배치 수행 시간 **4시간 → 35분**
- 정산 오류 재처리 리드타임 **익영업일 → 자동 즉시 재처리**
- 수동 크론 관리 포인트 **8개 → 0개**
- 크레딧 일괄 충전 처리 시간 **1시간 → 20분** (66% 개선)

---

### 4. 금결원 보고 대응 — 크레딧 만료 처리 및 낙전수입 인식 체계 구축

> Spring Boot, Spring Batch, JPA, EventBridge

**Situation**
크레딧 만료 처리가 체계화되지 않아 만료 대상 크레딧이 잔액에 그대로 남아 있는 상태. 금결원 보고 시 낙전수입 인식 시점이 불명확하여 재무 보고 정확성에 리스크 발생.

**Task**
금결원 보고 요건을 충족하는 만료 처리 체계와 감사 추적 시스템을 구축.

**Action**
- **Spring Batch 일별 만료 스캔 Job 구현.** 만료일 도래 크레딧을 Chunk 단위로 스캔하여 상태 전환 처리. 실시간 이벤트 방식은 서비스 장애 시 누락 위험이 있어 배치 스캔 방식 채택.
- **만료 → 낙전수입 계정 전환 로직을 회계 기준과 정합.** 재무팀과 인식 시점 기준을 사전 합의하여 보고 시 해석 차이를 제거.
- **EventBridge 스케줄링 + 감사 로그로 추적 체계 확보.** 만료 처리 건별 감사 로그를 기록하여 금결원 보고 시 건별 추적 가능한 데이터 체계 구축.

**Result**
- 미처리 만료 크레딧 **2,400건 → 0건**
- 금결원 보고 시 낙전수입 인식 정확도 **100%**
- 만료 처리 감사 추적 **수동 → 자동화**

---

### 5. 통합 메시징 플랫폼 구축 (프로젝트 리드)

> Kotlin, Spring Boot, JPA, gRPC, Protobuf, MQTT(EMQX), GitHub Packages

**Situation**
SMS, LMS, 앱 푸시, 이메일, 알림톡 등 13개 이상의 서비스에서 각자 벤더 API를 직접 호출하는 구조. 채널 추가·벤더 변경 시 전 서비스 수정이 필요하고, 발송량 제어가 불가능하여 비용 초과 및 장애 전파 리스크 상존.

**Task**
벤더 의존성을 서비스 코드에서 완전히 제거하고, 채널 추가·변경이 한 곳에서 완결되는 플랫폼 구조를 설계.

**Action**
- **멀티모듈 구조로 채널 추상화 계층 설계.** 벤더 연동(세종텔레콤, Firebase FCM, AWS SES)을 채널 어댑터 모듈로 분리하여 벤더 교체 시 어댑터만 교체하는 구조 확보.
- **서비스별 일일·월별 Rate Limit 정책 수립.** 13개 서비스의 과거 발송량을 분석하여 피크 볼륨 대비 배수로 쿼터를 산정. 버스트 특성 서비스는 Token Bucket에 30% 버스트 헤드룸 적용.
- **MQTT 브로커(EMQX)를 디스패치 레이어로 도입.** 기존 Kinesis는 데이터·로그 파이프라인으로 유지하고, 메시지 발송 디스패치는 EMQX로 분리. 토픽 기반 라우팅이 채널별 발송 분기에 적합.
- **gRPC/Protobuf 기반 내부 SDK를 GitHub Packages로 배포.** REST 대비 페이로드 크기와 직렬화 비용 절감. 서비스 측은 SDK 의존성 추가만으로 메시징 연동 완료.

**Result**
- 신규 채널 추가 리드타임 **3주 → 어댑터 모듈 1개 추가(3일)**
- 벤더 장애 시 타 채널 영향 **있음 → 완전 격리**
- 서비스별 메시징 연동 코드 **~300줄 → SDK 의존성 1줄**

---

### 6. JIRA 티켓 정량 감소를 통한 엔지니어링 집중도 향상

> Spring Boot, Spring Batch, AWS SQS, EventBridge

**Situation**
개발팀의 JIRA 티켓을 유형별로 분석한 결과, 상당수가 데이터 보정, 수기 발송, 설정 변경 등 반복 운영성 요청. 자동화되지 않은 수동 작업이 티켓으로 유입되는 구조가 문제.

**Task**
반복 운영 요청을 시스템 안으로 흡수하여 엔지니어의 핵심 개발 시간을 확보.

**Action**
- **티켓 유형별 발생 빈도 분석 후 자동화 대상 선정.** 최근 6개월 티켓을 카테고리별로 분류하여 자동화 ROI가 높은 유형부터 순차 전환.
- **수동 요청을 배치 자동화 또는 셀프서비스 어드민으로 전환.** 데이터 보정 요청은 Spring Batch Job으로, 설정 변경 요청은 어드민 UI 셀프서비스로 전환. SQS 이벤트 기반 트리거로 요청 접수 → 자동 처리 파이프라인 구축.
- **EventBridge 스케줄 배치로 주기적 운영 작업 자동화.** 정기적 운영 작업(리포트 생성, 상태 동기화 등)을 스케줄 배치로 전환하여 티켓 발생 자체를 제거.

**Result**
- 월간 운영 JIRA 티켓 **160건 → 35건** (78% 감소)
- 엔지니어 운영 대응 시간 **주 14시간 → 3시간**

---

## Professional Experience

### SK일렉링크 — 백엔드 엔지니어
**2023.01 — 현재** · 서울 송파구

전기차 충전 인프라를 기반으로 충전 슈퍼앱, 차량 진단 O2O, 데이터 기반 핀테크 등 대용량 트래픽을 처리하는 모빌리티 생태계를 구축하는 그린 모빌리티 플랫폼 기업.

**테크팀 — 플랫폼 셀 (2026.03 — 현재)**
- 회원 유형별 가격·할인 규칙이 하나의 정산 흐름에 누적되며 변경 영향 범위가 커지던 문제를 해결하기 위해, 정산 정책을 회원 유형별(운수사, 크루 멤버십, 구독 멤버십)로 격리하는 구조 설계.
- 통합 메시징 플랫폼을 설계하고, 공통 계약을 SDK로 추상화해 Node.js·Python·JVM 환경에서 재사용할 수 있도록 GitHub Packages로 제공.
- 정산/미결제 재처리 스케줄러를 Java+MyBatis에서 Kotlin+JPA/QueryDSL 구조로 전환하고, 이벤트 드리븐 아키텍처로 재설계.

**플랫폼 개발팀 (2023.01 — 2026.02)**
- 크레딧·마일리지 API의 폐쇄망 이관 시 카드번호 기준을 멤버 아이디 기준으로 전환하며 데이터 모델과 API 경계를 재설계.
- AWS SQS와 EventBridge Scheduler를 활용한 만료 배치 처리 기능을 구축하고, 비동기 만료 로직 설계.
- 금융 도메인에서 SQL Injection 위험의 String 기반 쿼리와 Double Writing 로직을 제거해 데이터 정합성 확보.
- B2B 크레딧 유형(제네시스 제휴) 분리 및 전용 충전·차감·조회 API 구현.

### 에스트래픽 — 백엔드 엔지니어
**2022.05 — 2022.12** · 경기 성남시 (DT추진실)

전기차 충전 운영 시스템과 크레딧 서비스를 개발·운영하던 조직. 2023년 SK일렉링크로 인수합병.

- 원장 기반 크레딧 API를 구축하고, 부분·전액 잔액 이전, 수수료 부과 취소, 전체·부분 취소 기능을 금액 상태 변화가 추적 가능하도록 설계.
- B2B 고객별 운영 정책 차이를 동일 시스템 안에서 흡수할 수 있도록 크레딧 유형 추가, 유효기간 연장, 일괄 충전 기능을 API와 배치로 구현.

### 모바일엔트로피 — 백엔드 엔지니어
**2019.04 — 2022.04** · 서울 마포구

도시가스 고객사의 자산·현장 업무 시스템을 개발·운영하던 소프트웨어 기업.

- 고객사별로 달랐던 자산·공사 운영 기준을 시스템으로 표준화하기 위해 배관·자재·계량기 관리 ERP 기능과 데이터 처리 로직을 개발·유지보수.
- 현장 검침·점검 데이터를 즉시 기록할 수 있도록 PDA 기반 웹앱을 개발하고, 카카오톡 알림톡 기반 인터랙티브 플랫폼을 구현.

---

## Education

| 학교 | 전공 | 기간 |
|------|------|------|
| 한국방송통신대학교 | 컴퓨터과학과 | 2020.03 — 재학 중 |
| 인덕대학교 | 비즈니스일본어과 졸업 | 2015.03 — 2017.02 |
